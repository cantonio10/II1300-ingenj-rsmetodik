task main() {
  float distanceToWall;

  SetSensorUltrasonic(IN_2);
  distanceToWall = SensorUS(IN_2);



  while(distanceToWall > 30.0)
  {
    RotateMotorEx(OUT_BC, 50, 100, 0, true, true);
    distanceToWall = SensorUS(IN_2);
  }
}
