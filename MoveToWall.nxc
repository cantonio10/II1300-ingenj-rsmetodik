#include "Constants.nxc"

sub MoveToWall(int power){
  SetSensorUltrasonic(IN_2);
  do{
    RotateMotorEx(OUT_BC, power, 700, 0, true, true);
  }
  while (SensorUS(IN_2) > 50) {
    //Stay in loop until close to the wall
  }
  Off(WHEEL_MOTORS);
  /*
  until(SensorUS(IN_2) < 40){
    OnFwdSync(WHEEL_MOTORS, 30, 0);
  }
  Off(WHEEL_MOTORS);
  */
}
